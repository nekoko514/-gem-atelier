# Gem Atelier

A small WebGL gemstone viewer built as a single-page HTML app.

## Files

- `index.html` - the app entry point

## Notes

The app uses Three.js from CDNJS, so it needs network access when opened locally or through GitHub Pages.
